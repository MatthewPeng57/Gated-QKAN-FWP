# --- experiment outputs (created by training runs) ---------------------------
results/
result_*/
best*/
outputs/
runs/
logs/
temp*/
temp_smoke/

# --- checkpoints & large model artifacts -------------------------------------
*.pth
*.pt
*.ckpt
*.safetensors
*.pkl

# --- python ------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/
.venv/
venv/

# --- compiled CUDA / torch extensions ----------------------------------------
torch_extensions/
*.so
*.o

# --- secrets / credentials (never commit) ------------------------------------
.env
.env.*
*.key
*.pem
credentials.json
settings.json

# --- local agent / tooling config (not part of the project) ------------------
.claude/
CLAUDE.md
plan/
temp/

# --- os / editor noise -------------------------------------------------------
.DS_Store
Thumbs.db
*.swp
*~

# NOTE: data files are TRACKED on purpose and are small:
#   Solar_cycle_forecasting/data/Sunspots.csv      (71 KB, SILSO, CC BY-NC 4.0)
#   times_series_benchmark/cuda_q_data/*.csv       (148 KB, generated)
# See THIRD_PARTY_NOTICES.md section 8 for their licenses.
